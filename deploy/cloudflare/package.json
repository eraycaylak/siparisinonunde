{
  "name": "siparisinonunde-cloudflare-dev",
  "private": true,
  "type": "module",
  "description": "Cloudflare dev/demo dağıtımı: Worker + tek container (PostgreSQL + API + worker + web). Bkz. docs/15 §13.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7",
    "wrangler": "4.140.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260925.1",
    "typescript": "5.9.3"
  }
}
